[package]
name = "bo-core"
version = "0.1.0"
edition = "2024"
description = "bo's data model — the structures the engine, the daemon and the client are all built on"
keywords = ["audio", "daw", "cli", "agent", "mixer"]
categories = ["command-line-interface", "multimedia::audio"]
license = "BSD-3-Clause"

[dependencies]
serde_json = "1"
